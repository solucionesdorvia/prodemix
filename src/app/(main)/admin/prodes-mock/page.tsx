import { AdminProdesClient } from "@/components/admin/AdminProdesClient";

/** Prodes solo en localStorage (legado / pruebas). */
export default function AdminProdesMockPage() {
  return <AdminProdesClient />;
}
